node_modules/
artifacts/
artifacts-zk/
cache/
cache-zk/
typechain-types/
coverage/
coverage.json

.env
.env.*
!.env.example
deployer-address.txt
.deployer-address.txt
.ci-key

evaluation/data/tmp/
evaluation/data/*-simulated.json
evaluation/data/*-tmp.json

# Repository sample clones, ~1.2 GB. Rebuilt from recorded commit hashes with
# evaluation/sample-clone.sh, so the hashes are the archive, not the checkout.
sample/

# Local Syft binary used by scripts/sbom-coverage.ts. Version is pinned in
# evaluation/sbom-coverage.md; the binary is not the archive.
tools/

deployments/hardhat.json
deployments/localhost.json

*.log
npm-debug.log*

.DS_Store
Thumbs.db
